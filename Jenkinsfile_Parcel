Pipleline {
  ageny { lable 'java_node' }
  stages {
    stages ('checkout') {
      steps {
        sh 'git clone https://github.com/priya027v/Parcel-service.git'
      }
    }
   tools {
        jdk 'java-11'
        maven 'maven-3'
    } 
    stage ('Build') {
      steps {
        sh '''
        cd Parcel-Service
        git Checkout feature-1
        mvn clean install
        '''
      }
    }
  }
}
        
